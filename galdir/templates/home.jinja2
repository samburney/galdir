{% extends "layouts/default.jinja2" %} 

{% block content %}
    {% for item in items %}
<p>
        {% if item.type == 'dir' %}
    {{item.type}} <a href="/view/{{item.path}}">{{item.name}}</a>
        {% endif %}
        {% if item.type == 'file' %}
        {% endif %}
</p>
    {% endfor %}
<p>
    {% for item in items %}
        {% if item.type == 'file' %}
    <a href="/albums/{{item.path}}">
        <img src="/viewimage/{{item.path}}" />
    </a>
        {% endif %}
    {% endfor %}
</p>
{% endblock content %}